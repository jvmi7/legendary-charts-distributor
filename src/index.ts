/**
 * Legendary Charts Distributor
 * A Node.js TypeScript application
 *
 * @format
 */

// Example function with TypeScript types
function greet(name: string): string {
  return `Hello, ${name}! Welcome to Legendary Charts Distributor`;
}

// Log a message to show the app is running
console.log(greet('User'));

// Export for testing/importing
export { greet };
